# TODO

`@config/install_lazyvim.sh`

- Make the script more interactive by allowing the user to choose the platform Ubuntu/MacOS
- Remove log noise
- Make the UX nice

`@config/*`

- Change the non script files to scripts which automatically installs and place the config in the correct folder
